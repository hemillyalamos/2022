#include<stdio.h>
#include<locale.h>

	int main(){
	
	 setlocale(LC_ALL,"");
	 char name[20];
	 float preco;
	 
	 printf("Digite o nome da mercadoria (máx 20 caracteres sem espaço) \n");
	 scanf("%s",&name);
	 printf("Digite o preço da mercadoria: \n");
	 scanf("%f",&preco);
	 
	 if(preco )
	 }
